#include <stdio.h>
#include <math.h>
 
double quadratic(double a, double b, double c);


int main(void)
{
  double a =3, b=4, c=1;

  double y = quadratic(a,b,c);
 
  printf("%1f\n", y );

  double quadratic(double a, double b, double c);


  return (-b + sqrt(b*b - 4*a*c)) / (2*a);

}

